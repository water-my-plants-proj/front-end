import React from 'react';

const initialState = {
    plantList: [
            {}
        ],
    isFetching: false,
    error: '', 

}

export const Reducer = (state=initialState, action) => {
    switch(action.type) {
        case A:
            return state
        default:
            return state
    }
}